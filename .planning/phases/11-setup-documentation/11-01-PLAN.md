---
phase: 11-setup-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - Dockerfile
  - docker-compose.prod.yml
  - docs/docker.md
  - package.json
autonomous: true
requirements: [DOCS-01, DOCS-03]

must_haves:
  truths:
    - "README.md exists at repo root and explains what the project is in one paragraph"
    - "README quickstart gets a developer from clone to running server using Docker in under 10 commands"
    - "Production Dockerfile builds a working image with dashboard included"
    - "docker-compose.prod.yml starts postgres + app + migrations in one command"
    - "Docker guide walks through every step with annotated explanations"
  artifacts:
    - path: "README.md"
      provides: "Project overview, quickstart, links to guides"
      contains: "## Quickstart"
    - path: "Dockerfile"
      provides: "Production multi-stage Docker image"
      contains: "npm run build"
    - path: "docker-compose.prod.yml"
      provides: "Production compose with postgres, migrate, app services"
      contains: "migrate/migrate"
    - path: "docs/docker.md"
      provides: "Step-by-step Docker deployment guide"
      contains: "## Prerequisites"
    - path: "package.json"
      provides: "tsx moved to dependencies for production runtime"
      contains: "\"tsx\":"
  key_links:
    - from: "README.md"
      to: "docs/docker.md"
      via: "markdown link"
      pattern: "\\[.*\\]\\(docs/docker\\.md\\)"
    - from: "README.md"
      to: "docs/linux-vps.md"
      via: "markdown link"
      pattern: "\\[.*\\]\\(docs/linux-vps\\.md\\)"
    - from: "README.md"
      to: "docs/windows-server.md"
      via: "markdown link"
      pattern: "\\[.*\\]\\(docs/windows-server\\.md\\)"
    - from: "docker-compose.prod.yml"
      to: "Dockerfile"
      via: "build context reference"
      pattern: "dockerfile: Dockerfile"
    - from: "docs/docker.md"
      to: "docker-compose.prod.yml"
      via: "references compose file"
      pattern: "docker-compose\\.prod\\.yml"
---

<objective>
Create the project README and Docker deployment infrastructure (production Dockerfile, production docker-compose, and Docker deployment guide).

Purpose: DOCS-01 (README) and DOCS-03 (Docker guide) are paired because the README quickstart section uses Docker as the fastest path from clone to running server. The Dockerfile and docker-compose.prod.yml must exist as real files -- the Docker guide documents them.

Output: README.md, Dockerfile, docker-compose.prod.yml, docs/docker.md, updated package.json (tsx moved to dependencies)
</objective>

<execution_context>
@C:/Users/OEM/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OEM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-setup-documentation/11-RESEARCH.md

Source files to synthesize from:
@SETUP.md
@HOW_IT_WORKS.md
@.env.example
@Dockerfile.dev
@docker-compose.yml
@package.json
@dashboard/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create production Dockerfile, docker-compose.prod.yml, move tsx to dependencies, and write Docker guide</name>
  <files>Dockerfile, docker-compose.prod.yml, docs/docker.md, package.json</files>
  <read_first>
    - Dockerfile.dev (existing dev Dockerfile -- understand current pattern)
    - docker-compose.yml (existing dev compose -- understand current services)
    - package.json (current dependencies -- tsx is in devDependencies, must move)
    - dashboard/package.json (dashboard build needs its own npm ci)
    - SETUP.md (migration commands, post-migration grants, env var reference)
    - .env.example (all environment variables)
    - HOW_IT_WORKS.md (architecture overview, directory layout)
    - .planning/phases/11-setup-documentation/11-RESEARCH.md (production Dockerfile pattern, compose migration service pattern, pitfalls)
  </read_first>
  <action>
**Step 1: Move tsx from devDependencies to dependencies in package.json**

In `package.json`, remove `"tsx": "latest"` from `devDependencies` and add `"tsx": "latest"` to `dependencies`. This is required because the production start command (`node --import tsx/esm src/index.ts`) needs tsx at runtime. The resulting dependencies block should be:

```json
"dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/static": "^9.0.0",
    "@fastify/swagger": "^9.0.0",
    "@modelcontextprotocol/sdk": "^1.27.0",
    "@scalar/fastify-api-reference": "latest",
    "dotenv": "^16.0.0",
    "drizzle-orm": "^0.45.0",
    "fastify": "^5.0.0",
    "postgres": "^3.4.0",
    "tsx": "latest",
    "zod": "^3.25.0"
},
"devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0",
    "vitest": "latest"
}
```

**Step 2: Create `Dockerfile` (production)**

Create `Dockerfile` at repo root (NOT Dockerfile.dev -- that stays as-is for development). Contents:

```dockerfile
FROM node:22-alpine

WORKDIR /app

# Install server dependencies (tsx is in dependencies, not devDependencies)
COPY package*.json ./
RUN npm ci --omit=dev

# Install dashboard dependencies and build
COPY dashboard/package*.json ./dashboard/
RUN cd dashboard && npm ci
COPY dashboard/ ./dashboard/
RUN cd dashboard && npm run build

# Copy server source and config
COPY src/ ./src/
COPY tsconfig.json ./
COPY db/ ./db/

ENV NODE_ENV=production
EXPOSE 3000

CMD ["node", "--import", "tsx/esm", "src/index.ts"]
```

Key points:
- Uses `npm ci --omit=dev` for server deps (tsx is now in dependencies so it is included)
- Builds dashboard in a separate step (dashboard has its own package.json)
- Copies `db/` directory so migration files are available inside the container
- Does NOT run migrations -- that is handled by the separate migrate service in compose

**Step 3: Create `docker-compose.prod.yml`**

Create `docker-compose.prod.yml` at repo root. This is the production compose file (the existing `docker-compose.yml` remains for development). Contents:

```yaml
# Production docker-compose for PB MCP
# Usage: docker compose -f docker-compose.prod.yml up -d
#
# Before first run:
#   1. Copy .env.example to .env and fill in real values
#   2. Run migrations: docker compose -f docker-compose.prod.yml run --rm migrate
#   3. Set app_login password and grants (see docs/docker.md)

services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: pb_mcp
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  migrate:
    image: migrate/migrate:v4.19.1
    volumes:
      - ./db/migrations:/migrations
    command:
      - "-path"
      - "/migrations"
      - "-database"
      - "postgres://postgres:${POSTGRES_PASSWORD:-postgres}@postgres:5432/pb_mcp?sslmode=disable"
      - "up"
    depends_on:
      postgres:
        condition: service_healthy
    profiles:
      - migrate

  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    environment:
      - DATABASE_URL=postgres://app_login:${APP_LOGIN_PASSWORD:-changeme}@postgres:5432/pb_mcp
      - DATABASE_MIGRATION_URL=postgres://postgres:${POSTGRES_PASSWORD:-postgres}@postgres:5432/pb_mcp
    ports:
      - "${PORT:-3000}:3000"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

volumes:
  postgres_data:
```

Key design decisions:
- `migrate` service uses `profiles: [migrate]` so it does NOT run on `docker compose up` -- it must be invoked explicitly with `docker compose --profile migrate run --rm migrate`
- `POSTGRES_PASSWORD` is extracted as a variable so it can be set in .env
- `APP_LOGIN_PASSWORD` is extracted so the DATABASE_URL password can match what the user sets in the post-migration step
- The app service uses `env_file: .env` for all other env vars (ADMIN_SECRET, JWT_SECRET, etc.)

**Step 4: Create `docs/` directory and write `docs/docker.md`**

Create directory `docs/` if it does not exist, then create `docs/docker.md` with the following structure and content:

```markdown
# Docker Deployment Guide

How to deploy PB MCP using Docker and Docker Compose. This is the fastest path from clone to running server.

---

## Prerequisites

| Requirement | Version | Check |
|-------------|---------|-------|
| Docker Engine | 24+ | `docker --version` |
| Docker Compose | v2+ (included with Docker Desktop) | `docker compose version` |
| Git | any | `git --version` |

---

## Step 1 -- Clone the repository

\```bash
git clone <your-repo-url> pb-mcp
cd pb-mcp
\```

---

## Step 2 -- Create the .env file

\```bash
cp .env.example .env
\```

Open `.env` and set these values:

| Variable | What to set | How to generate |
|----------|------------|-----------------|
| `POSTGRES_PASSWORD` | PostgreSQL superuser password | `openssl rand -hex 16` |
| `APP_LOGIN_PASSWORD` | Application database user password | `openssl rand -hex 16` |
| `ADMIN_SECRET` | Admin API + dashboard login password | `openssl rand -hex 32` |
| `JWT_SECRET` | JWT signing key | `openssl rand -hex 32` |

The remaining variables have sensible defaults. YouTrack variables are optional -- the server starts without them.

> **Important:** `APP_LOGIN_PASSWORD` must match the password you set in Step 4 below.

---

## Step 3 -- Start PostgreSQL

\```bash
docker compose -f docker-compose.prod.yml up -d postgres
\```

Wait for the health check to pass:

\```bash
docker compose -f docker-compose.prod.yml ps
# postgres should show "healthy"
\```

---

## Step 4 -- Run database migrations

\```bash
docker compose -f docker-compose.prod.yml --profile migrate run --rm migrate
\```

This runs all 10 migrations (roles, tables, RLS policies). Expected output ends with `no change`.

**Set the app_login password** (must match `APP_LOGIN_PASSWORD` in your `.env`):

\```bash
docker compose -f docker-compose.prod.yml exec postgres \
  psql -U postgres -d pb_mcp -c "ALTER ROLE app_login PASSWORD 'YOUR_APP_LOGIN_PASSWORD';"
\```

**Grant table permissions:**

\```bash
docker compose -f docker-compose.prod.yml exec postgres \
  psql -U postgres -d pb_mcp -c "
    GRANT SELECT, INSERT, UPDATE ON ALL TABLES IN SCHEMA public TO app_user;
    GRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA public TO app_user;
  "
\```

---

## Step 5 -- Build and start the application

\```bash
docker compose -f docker-compose.prod.yml up -d app
\```

The first run builds the Docker image (installs dependencies, builds the dashboard). This takes 1-2 minutes.

Check logs:

\```bash
docker compose -f docker-compose.prod.yml logs -f app
\```

Expected output (on stderr):

\```
[kb/scheduler] Starting KB auto-sync every 1800000ms
[pb-mcp] Server listening on http://0.0.0.0:3000
[pb-mcp] Admin UI: http://0.0.0.0:3000/docs
\```

> **Note:** The server prints nothing to stdout. All output goes to stderr. This is required -- MCP uses stdout as its wire protocol.

---

## Step 6 -- Verify the deployment

Open the admin dashboard:

\```
http://localhost:3000/dashboard/
\```

Log in with username `admin` (or your `ADMIN_USERNAME`) and the `ADMIN_SECRET` value as the password.

Create your first tenant using the dashboard or curl:

\```bash
curl -s -X POST http://localhost:3000/admin/tenants \
  -H "Content-Type: application/json" \
  -H "X-Admin-Secret: YOUR_ADMIN_SECRET" \
  -d '{"name": "Acme Corp", "slug": "acme", "plan": "standard"}' | jq .
\```

Save the returned `apiKey` -- it is shown only once.

---

## Step 7 -- Connect an AI client

Add to your Claude Desktop `claude_desktop_config.json`:

\```json
{
  "mcpServers": {
    "pb-mcp": {
      "url": "http://YOUR_SERVER_IP:3000/mcp",
      "headers": {
        "X-Api-Key": "pb_your_api_key_here"
      }
    }
  }
}
\```

Replace `YOUR_SERVER_IP` with your server's IP or hostname.

---

## Updating

To update to a new version:

\```bash
git pull
docker compose -f docker-compose.prod.yml build app
docker compose -f docker-compose.prod.yml --profile migrate run --rm migrate
docker compose -f docker-compose.prod.yml up -d app
\```

---

## Stopping and cleanup

\```bash
# Stop all services
docker compose -f docker-compose.prod.yml down

# Stop and remove data volumes (destroys database!)
docker compose -f docker-compose.prod.yml down -v
\```

---

## Troubleshooting

### "password authentication failed for user app_login"
The password in `DATABASE_URL` (set via `APP_LOGIN_PASSWORD` in `.env`) does not match the role password. Re-run the ALTER ROLE command from Step 4.

### "permission denied for table products"
The grants from Step 4 are missing. Re-run the GRANT commands.

### Dashboard returns 404
The Docker image may not have built the dashboard. Rebuild: `docker compose -f docker-compose.prod.yml build --no-cache app`

### Container keeps restarting
Check logs: `docker compose -f docker-compose.prod.yml logs app`. Common cause: missing required env vars (`DATABASE_URL`, `ADMIN_SECRET`, `JWT_SECRET`).
```

The guide should use `\`` replaced with actual triple backticks (the backslash escapes above are for the plan template only).
  </action>
  <verify>
    <automated>test -f Dockerfile && test -f docker-compose.prod.yml && test -f docs/docker.md && grep -q '"tsx"' package.json | head -1 && echo "All Docker artifacts exist"</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile exists at repo root (NOT Dockerfile.dev -- that must still exist unchanged)
    - Dockerfile contains `FROM node:22-alpine`
    - Dockerfile contains `npm ci --omit=dev`
    - Dockerfile contains `npm run build` (dashboard build step)
    - Dockerfile contains `CMD ["node", "--import", "tsx/esm", "src/index.ts"]`
    - Dockerfile contains `COPY db/ ./db/`
    - docker-compose.prod.yml exists at repo root
    - docker-compose.prod.yml contains `image: postgres:17-alpine`
    - docker-compose.prod.yml contains `image: migrate/migrate:v4.19.1`
    - docker-compose.prod.yml contains `profiles:` with `migrate` (so migrate does not auto-run)
    - docker-compose.prod.yml contains `dockerfile: Dockerfile`
    - docker-compose.prod.yml contains `env_file: .env`
    - docs/docker.md exists
    - docs/docker.md contains `## Prerequisites`
    - docs/docker.md contains `## Step 1`
    - docs/docker.md contains `ALTER ROLE app_login`
    - docs/docker.md contains `GRANT SELECT, INSERT, UPDATE`
    - docs/docker.md contains `## Troubleshooting`
    - package.json has `"tsx": "latest"` in dependencies block (not devDependencies)
    - package.json devDependencies does NOT contain tsx
    - Dockerfile.dev still exists unchanged
    - docker-compose.yml (dev) still exists unchanged
  </acceptance_criteria>
  <done>Production Dockerfile builds a complete image with dashboard, docker-compose.prod.yml orchestrates postgres + migrate + app services, Docker guide covers clone-to-running in 7 annotated steps with troubleshooting, tsx is a production dependency</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md with project overview and Docker quickstart</name>
  <files>README.md</files>
  <read_first>
    - HOW_IT_WORKS.md (architecture overview, technology stack table, tool list, directory layout -- synthesize into README overview)
    - SETUP.md (step-by-step reference -- README links to this)
    - docs/docker.md (just created in Task 1 -- README quickstart mirrors the Docker path)
    - docker-compose.prod.yml (just created -- README quickstart references it)
    - .env.example (env vars for quickstart)
    - .planning/phases/11-setup-documentation/11-RESEARCH.md (README structure guidance, MCP transport note)
  </read_first>
  <action>
Create `README.md` at repository root with the following structure and content:

**Section 1: Title and badges**
```
# PB MCP -- Multi-Tenant ERP MCP Server
```

One-line description: "Give AI assistants access to your ERP data. Multi-tenant, PostgreSQL-backed, MCP Streamable HTTP."

**Section 2: What is this? (## Overview)**

Write one paragraph (3-4 sentences) answering: what does this server do, who is it for, what protocol does it use. Key points to include:
- Exposes POSibolt ERP operations (inventory, orders, invoices, contacts) and YouTrack knowledge base articles as MCP tools
- Multi-tenant: one server instance, multiple companies, data isolated by PostgreSQL Row-Level Security
- AI assistants (Claude, Cursor, any MCP client) connect via MCP Streamable HTTP transport
- Admin dashboard (React) for managing tenants, API keys, tool permissions, and audit logs

**Section 3: What can an AI assistant do? (## Available Tools)**

Table listing tool categories and counts:
| Category | Tools | Examples |
|----------|-------|----------|
| Inventory | 7 | list_products, get_product, search_products, list_stock_levels, ... |
| Orders & Invoices | 6 | list_orders, get_order, list_invoices, get_invoice, ... |
| CRM / Contacts | 5 | list_contacts, search_contacts, get_contact, ... |
| Knowledge Base | 3 | search_kb, get_kb_article, get_kb_sync_status |
| **Total** | **21** | |

Note: "Tool access is configurable per-tenant and per-API-key."

**Section 4: Architecture summary (## Architecture)**

Brief (5-7 lines) covering:
- Stack: Node.js 22, Fastify 5, PostgreSQL 17, MCP SDK
- Tenant isolation: PostgreSQL RLS (Row-Level Security) on shared schema
- Auth: API-key-per-tenant (SHA-256 hashed at rest), optional expiry
- Dashboard auth: JWT (HS256), 8-hour default expiry
- Transport: MCP Streamable HTTP (not stdio) -- all output to stderr, stdout is the MCP wire protocol
- All 21 tool calls are audit-logged

Link: "See [HOW_IT_WORKS.md](HOW_IT_WORKS.md) for the full technical reference."

**Section 5: Quickstart (## Quickstart)**

Docker-based quickstart (fastest path). 6 commands:

```bash
git clone <your-repo-url> pb-mcp
cd pb-mcp
cp .env.example .env
# Edit .env: set POSTGRES_PASSWORD, APP_LOGIN_PASSWORD, ADMIN_SECRET, JWT_SECRET
# (generate secrets with: openssl rand -hex 32)

# Start PostgreSQL
docker compose -f docker-compose.prod.yml up -d postgres

# Run migrations
docker compose -f docker-compose.prod.yml --profile migrate run --rm migrate

# Set app_login password (must match APP_LOGIN_PASSWORD in .env)
docker compose -f docker-compose.prod.yml exec postgres \
  psql -U postgres -d pb_mcp -c "ALTER ROLE app_login PASSWORD 'your-app-login-password';"

# Grant table permissions
docker compose -f docker-compose.prod.yml exec postgres \
  psql -U postgres -d pb_mcp -c "
    GRANT SELECT, INSERT, UPDATE ON ALL TABLES IN SCHEMA public TO app_user;
    GRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA public TO app_user;"

# Build and start
docker compose -f docker-compose.prod.yml up -d app
```

Then: "Open `http://localhost:3000/dashboard/` to create your first tenant."

Link: "See the [Docker guide](docs/docker.md) for the full annotated walkthrough."

**Section 6: Connecting an AI client (## Connecting an AI Client)**

Show configuration for Claude Desktop:
```json
{
  "mcpServers": {
    "pb-mcp": {
      "url": "http://YOUR_SERVER:3000/mcp",
      "headers": {
        "X-Api-Key": "pb_your_api_key_here"
      }
    }
  }
}
```

Note: "This server uses MCP Streamable HTTP transport. Use the `url` field, not `command`/`args`."

For Cursor: same JSON structure, placed in Cursor's MCP configuration.

For any MCP client: "Connect to `http://YOUR_SERVER:3000/mcp` with header `X-Api-Key: pb_...`"

**Section 7: Deployment guides (## Deployment Guides)**

Bulleted list linking to each guide:
- [Docker](docs/docker.md) -- recommended for most deployments
- [Linux / VPS (Ubuntu/Debian)](docs/linux-vps.md) -- bare metal or VPS
- [Windows Server](docs/windows-server.md) -- Windows-specific setup
- [Generic setup (any platform)](SETUP.md) -- detailed step-by-step for all platforms

**Section 8: Admin operations (## Admin Dashboard)**

Brief paragraph: the dashboard at `/dashboard/` provides tenant management, API key management (with expiry and per-key tool scoping), tool permissions, ERP configuration, audit logs, knowledge base management, and PDF export.

Link: "See [SETUP.md](SETUP.md#admin-operations-reference) for the full admin API reference."

**Section 9: Technology stack table (## Stack)**

Copy the technology stack table from HOW_IT_WORKS.md (the compact one with Layer/Technology columns).

Add dashboard row: `Dashboard | React 19, Vite, Tailwind CSS v4`

**Section 10: License**

```
## License

[Add your license here]
```
  </action>
  <verify>
    <automated>test -f README.md && grep -c "##" README.md | head -1 && echo "README exists with sections"</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists at repo root
    - README.md contains `# PB MCP` as the title
    - README.md contains `## Overview` section
    - README.md contains `## Available Tools` section with a table
    - README.md contains `## Architecture` section
    - README.md contains `## Quickstart` section
    - README.md contains `docker compose -f docker-compose.prod.yml` in quickstart commands
    - README.md contains `ALTER ROLE app_login` in quickstart
    - README.md contains `GRANT SELECT, INSERT, UPDATE` in quickstart
    - README.md contains `## Connecting an AI Client` section
    - README.md contains `"url": "http://YOUR_SERVER:3000/mcp"` (Streamable HTTP, not stdio)
    - README.md contains `X-Api-Key` header in client config
    - README.md contains `## Deployment Guides` section
    - README.md contains links to `docs/docker.md`, `docs/linux-vps.md`, `docs/windows-server.md`, and `SETUP.md`
    - README.md contains `## Stack` section with technology table
    - README.md contains `HOW_IT_WORKS.md` link
    - README.md does NOT contain `command` or `args` in MCP client config (stdio pattern -- wrong for this server)
    - README.md contains stderr/stdout note or callout
  </acceptance_criteria>
  <done>README.md provides a clear project overview, tool summary, Docker-based quickstart from clone to running server, MCP client configuration for Claude Desktop/Cursor/generic, and links to all deployment guides and technical reference</done>
</task>

</tasks>

<verification>
Overall plan verification:

1. `test -f README.md` -- README exists
2. `test -f Dockerfile` -- Production Dockerfile exists
3. `test -f Dockerfile.dev` -- Dev Dockerfile still exists (not overwritten)
4. `test -f docker-compose.prod.yml` -- Production compose exists
5. `test -f docker-compose.yml` -- Dev compose still exists (not overwritten)
6. `test -f docs/docker.md` -- Docker guide exists
7. `grep -q '"tsx"' package.json` -- tsx is listed (verify it is in dependencies, not devDependencies)
8. `grep -q "## Quickstart" README.md` -- README has quickstart section
9. `grep -q "migrate/migrate" docker-compose.prod.yml` -- Compose uses official migrate image
10. `grep -q "ALTER ROLE app_login" docs/docker.md` -- Docker guide includes critical post-migration step
11. `npm test` -- existing test suite still passes (no server code broken)
</verification>

<success_criteria>
- README.md exists with overview, quickstart, client config, guide links, and stack table
- Production Dockerfile creates a working image (node:22-alpine, dashboard built, tsx available)
- docker-compose.prod.yml defines postgres, migrate (profiled), and app services
- Docker guide covers clone-to-running in annotated steps with troubleshooting
- tsx moved from devDependencies to dependencies in package.json
- All existing dev files (Dockerfile.dev, docker-compose.yml) are untouched
- `npm test` passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/11-setup-documentation/11-01-SUMMARY.md`
</output>
