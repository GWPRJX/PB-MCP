---
phase: 02-tenant-mcp-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tests/admin/tenant-crud.test.ts
  - tests/admin/api-keys.test.ts
  - tests/mcp/auth.test.ts
  - tests/mcp/transport.test.ts
autonomous: true
requirements:
  - TENANT-01
  - TENANT-02
  - TENANT-03
  - TENANT-04
  - TENANT-05
  - TENANT-06
  - TENANT-07
  - INFRA-02

must_haves:
  truths:
    - "npm install succeeds with all new runtime dependencies added to package.json"
    - "vitest run exits 0 after Wave 1 — all new stubs are it.todo, no failures"
    - "node -e require check confirms fastify, drizzle-orm, zod, @modelcontextprotocol/sdk in node_modules"
    - "All 4 new test files exist in tests/admin/ and tests/mcp/ with it.todo stubs"
  artifacts:
    - path: "package.json"
      provides: "updated dependencies block with fastify, drizzle-orm, zod, @modelcontextprotocol/sdk, @fastify/swagger, @scalar/fastify-api-reference"
      contains: "fastify"
    - path: "tests/admin/tenant-crud.test.ts"
      provides: "stubs for TENANT-01, TENANT-02, TENANT-03 — it.todo, vitest exits 0"
      contains: "it.todo"
    - path: "tests/admin/api-keys.test.ts"
      provides: "stubs for TENANT-04, TENANT-05 — it.todo, vitest exits 0"
      contains: "it.todo"
    - path: "tests/mcp/auth.test.ts"
      provides: "stubs for TENANT-06, TENANT-07 — it.todo, vitest exits 0"
      contains: "it.todo"
    - path: "tests/mcp/transport.test.ts"
      provides: "stub for INFRA-02 — it.todo, vitest exits 0"
      contains: "it.todo"
  key_links:
    - from: "package.json"
      to: "node_modules/fastify"
      via: "npm install"
      pattern: "fastify"
    - from: "tests/admin/tenant-crud.test.ts"
      to: "src/server.ts"
      via: "will import in Wave 3"
      pattern: "buildServer"
---

<objective>
Install all Phase 2 runtime dependencies and create all four test stub files so that every subsequent plan's `<automated>` verify command has a file to run against from its first commit.

Purpose: Wave 2, 3, and 4 plans all reference test files in `tests/admin/` and `tests/mcp/`. Those files must exist before implementation begins, or every verify command fails with "file not found". This plan installs packages and creates stubs — no implementation code yet.

Output: Updated package.json with 6 new runtime dependencies, 4 test stub files (all `it.todo`), `npm install` succeeds, `vitest run` exits 0.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tenant-mcp-shell/02-CONTEXT.md
@.planning/phases/02-tenant-mcp-shell/02-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install Phase 2 runtime dependencies</name>
  <files>package.json</files>
  <action>
Add the following runtime dependencies to `package.json` and run `npm install`.

**Exact versions to add to `"dependencies"`:**
```json
"fastify": "^5.0.0",
"@fastify/swagger": "^9.0.0",
"@scalar/fastify-api-reference": "latest",
"@modelcontextprotocol/sdk": "^1.27.0",
"zod": "^3.25.0",
"drizzle-orm": "^0.45.0"
```

The existing `"dependencies"` block already has `"dotenv": "^16.0.0"` and `"postgres": "^3.4.0"`. Merge the new entries — do not replace the existing ones.

Do NOT add `drizzle-kit` — it is NOT used in this project (migrations are plain SQL, not drizzle-kit generated).

Also add an `"admin:secret"` test helper script to `"scripts"`:
```json
"test:admin": "vitest run --reporter=verbose tests/admin/",
"test:mcp": "vitest run --reporter=verbose tests/mcp/"
```

After editing package.json, run `npm install` to produce the updated `package-lock.json` and populate `node_modules`.

After install, verify with:
```bash
node -e "
const deps = ['fastify','drizzle-orm','zod','@modelcontextprotocol/sdk','@fastify/swagger'];
deps.forEach(d => { require.resolve(d); process.stderr.write('[check] ' + d + ' OK\n'); });
"
```
All 5 packages must resolve without error. Note: `@scalar/fastify-api-reference` may not be resolvable via `require.resolve` (ESM package) — that's acceptable, verify its directory exists in `node_modules/@scalar/` instead.
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); ['fastify','drizzle-orm','zod','@modelcontextprotocol/sdk','@fastify/swagger','@scalar/fastify-api-reference'].forEach(d=>{ if(!p.dependencies[d]) throw new Error('missing dep: '+d); }); console.log('package.json deps OK')" && ls node_modules/fastify node_modules/drizzle-orm node_modules/zod 2>/dev/null && echo "node_modules OK"</automated>
  </verify>
  <done>
    - package.json dependencies block contains all 6 new packages at specified version ranges
    - existing dependencies (dotenv, postgres) are unchanged
    - node_modules/fastify, node_modules/drizzle-orm, node_modules/zod directories exist
    - package-lock.json updated
    - test:admin and test:mcp scripts added to package.json scripts block
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Test stubs for admin and MCP test suites</name>
  <files>
    tests/admin/tenant-crud.test.ts
    tests/admin/api-keys.test.ts
    tests/mcp/auth.test.ts
    tests/mcp/transport.test.ts
  </files>
  <action>
Create all four test stub files using `it.todo()` exclusively. Stubs must pass `vitest run` immediately (exit 0, zero failures). Use `describe` blocks that name the requirement each group covers. Do NOT use empty `it()` bodies — only `it.todo()`.

**tests/admin/tenant-crud.test.ts** — stubs for TENANT-01, TENANT-02, TENANT-03:
```typescript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';

// Tests require: running Fastify server (buildServer()), DATABASE_URL set, ADMIN_SECRET set
// Implemented in plan 02-03 (Wave 3) after admin routes exist.
// Uses fastify.inject() — no real HTTP port needed.

describe('POST /admin/tenants — create tenant (TENANT-01)', () => {
  it.todo('returns 201 with { tenantId, apiKey } on valid { name, slug, plan }');
  it.todo('apiKey begins with pb_ prefix and is 67 chars total');
  it.todo('raw apiKey is not stored in DB — only the SHA-256 hash');
  it.todo('returns 409 Conflict when slug already exists');
  it.todo('returns 400 when name is missing');
  it.todo('returns 400 when slug is missing');
  it.todo('returns 401 when X-Admin-Secret header is wrong');
  it.todo('returns 401 when X-Admin-Secret header is missing');
});

describe('GET /admin/tenants — list tenants (TENANT-02)', () => {
  it.todo('returns 200 with array of tenants including status and keyCount');
  it.todo('returns 401 when X-Admin-Secret is missing');
  it.todo('newly created tenant appears in list');
  it.todo('keyCount reflects number of active keys for tenant');
});

describe('GET /admin/tenants/:id — get tenant detail (TENANT-03)', () => {
  it.todo('returns 200 with full tenant object including apiKeys array');
  it.todo('apiKeys array contains key metadata but NOT key_hash or raw key');
  it.todo('returns 404 when tenant ID does not exist');
  it.todo('returns 401 when X-Admin-Secret is missing');
});
```

**tests/admin/api-keys.test.ts** — stubs for TENANT-04, TENANT-05:
```typescript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';

// Tests require: running Fastify server, admin routes from plan 02-03
// Implemented in plan 02-03 (Wave 3).

describe('POST /admin/tenants/:id/keys — issue API key (TENANT-04)', () => {
  it.todo('returns 201 with { keyId, apiKey } — raw key shown once');
  it.todo('apiKey begins with pb_ prefix');
  it.todo('new key appears in GET /admin/tenants/:id with status active');
  it.todo('key_hash in DB is SHA-256 of raw key');
  it.todo('returns 404 when tenant does not exist');
  it.todo('returns 401 when X-Admin-Secret is missing');
  it.todo('optional label field is stored when provided');
});

describe('DELETE /admin/tenants/:id/keys/:keyId — revoke API key (TENANT-05)', () => {
  it.todo('returns 204 on successful revocation');
  it.todo('revoked key immediately returns 401 on MCP request');
  it.todo('revoked key shows status=revoked in GET /admin/tenants/:id');
  it.todo('revoked_at timestamp is set on the key record');
  it.todo('returns 404 when key does not exist for this tenant');
  it.todo('returns 401 when X-Admin-Secret is missing');
  it.todo('cannot revoke a key that is already revoked (404)');
});
```

**tests/mcp/auth.test.ts** — stubs for TENANT-06, TENANT-07:
```typescript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';

// Tests require: Fastify server with MCP routes from plan 02-04 (Wave 4)
// Uses fastify.inject() to send MCP initialize requests.

describe('MCP auth — X-Api-Key header validation (TENANT-06)', () => {
  it.todo('valid API key in X-Api-Key header returns 200 with MCP initialize response');
  it.todo('missing X-Api-Key header returns 401 with error message');
  it.todo('invalid (non-existent) API key returns 401');
  it.todo('revoked API key returns 401 immediately after revocation');
  it.todo('malformed key (wrong format) returns 401');
});

describe('MCP auth — tenant_id resolution and RLS (TENANT-07)', () => {
  it.todo('authenticated request resolves API key to correct tenant_id');
  it.todo('tenant_id is stored in AsyncLocalStorage for the request lifecycle');
  it.todo('DB transaction within authenticated request has app.current_tenant_id set');
  it.todo('two concurrent requests with different keys resolve to different tenant_ids');
});
```

**tests/mcp/transport.test.ts** — stub for INFRA-02:
```typescript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';

// Tests require: Fastify server with MCP routes from plan 02-04 (Wave 4)
// Verifies Streamable HTTP transport per MCP spec (not stdio).

describe('MCP Streamable HTTP transport (INFRA-02)', () => {
  it.todo('POST /mcp accepts application/json and returns application/json');
  it.todo('POST /mcp with valid initialize request returns MCP protocol response');
  it.todo('GET /mcp returns SSE stream (text/event-stream content-type)');
  it.todo('DELETE /mcp returns 200 session termination');
  it.todo('tools/list returns empty array before any tools are registered');
  it.todo('MCP server does NOT write to stdout (stderr only)');
});
```

After creating all 4 files, run `npx vitest run` and confirm it exits 0.
  </action>
  <verify>
    <automated>ls tests/admin/tenant-crud.test.ts tests/admin/api-keys.test.ts tests/mcp/auth.test.ts tests/mcp/transport.test.ts && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>
    - All 4 test files exist in the correct directories
    - Each file uses only it.todo() (no empty it() bodies)
    - vitest run exits 0 — zero failures across entire test suite
    - Each describe block names the requirement it covers (TENANT-01, etc.)
  </done>
</task>

</tasks>

<verification>
Run after both tasks complete:

1. `node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log(Object.keys(p.dependencies).join(', '))"` — output includes fastify, drizzle-orm, zod, @modelcontextprotocol/sdk
2. `npx vitest run 2>&1 | tail -5` — exits 0, shows "0 failed"
3. `ls tests/admin/ tests/mcp/` — shows all 4 test files
4. `grep -c "it.todo" tests/admin/tenant-crud.test.ts` — returns a positive number (no empty it() bodies)
</verification>

<success_criteria>
- package.json has all 6 new runtime dependencies at correct version ranges
- npm install succeeded — all packages in node_modules
- test:admin and test:mcp npm scripts added
- 4 test stub files exist with it.todo() only — vitest exits 0, zero failures
- No implementation code written yet — this plan is setup only
</success_criteria>

<output>
After completion, create `.planning/phases/02-tenant-mcp-shell/02-01-SUMMARY.md` with:
- Dependencies added (list each with resolved version)
- Test stub files created (list each with describe block count)
- Any deviations from this plan
</output>
