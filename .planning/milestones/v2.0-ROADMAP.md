# v2.0 — Admin Dashboard + Write Operations (Archived)

**Completed:** 2026-03-18
**Requirements:** 26/26 delivered
**Phases:** 3 | **Plans:** 7 | **Commits:** 24
**Lines:** +9,499 / -154 across 63 files

---

## Key Accomplishments

1. **Tool Access Control** — Per-tenant and per-key tool permissions with `shouldRegister()` guards across all 27 MCP tools
2. **Audit Logging** — Fire-and-forget `withAudit()` wrapper on all 27 tool handlers, queryable via admin API with filters/pagination
3. **JWT Dashboard Auth** — HS256 JWT signing with built-in Node.js crypto, login endpoint, dual-mode auth (JWT + backward-compat X-Admin-Secret)
4. **React Admin Dashboard** — Full SPA with 5 tabs (Keys, Tools, ERP Config, Audit Log, API Docs), served via @fastify/static
5. **KB Doc Management** — CRUD endpoints with DOC-* prefix, upload/edit/delete through dashboard, auto-searchable via existing KB tools
6. **6 MCP Write Tools** — Stock transfers, invoice creation/cancellation, contact CRUD via POSibolt POST API, all audit-logged

---

## Phase Details

### Phase 5: Backend Services

**Goal**: All v2 backend services are migrated, tested, and production-ready — tool access control filters MCP tools per tenant/key, audit log records every tool call, dashboard auth uses JWT, and API keys support expiry
**Depends on**: v1 complete (Phases 1-4)
**Requirements**: TAC-01, TAC-02, TAC-03, TAC-04, TAC-05, AUTH-01, AUTH-02
**Plans:** 3 plans

Plans:
- [x] 05-01-PLAN.md — Apply migrations, update CI RLS checks, wire audit logging into all MCP tool handlers
- [x] 05-02-PLAN.md — JWT auth login endpoint + middleware, API key expiry (migration 000009)
- [x] 05-03-PLAN.md — Integration tests for tool permissions, audit log, JWT auth, and key expiry

**Existing code**: Significant v2 backend code already existed in working tree (tool-permissions-service, audit-service, connection-tester, updated auth middleware, updated context). Migrations 000007 + 000008 written but not applied. Audit logging written but not wired into tool handlers.
**Success Criteria** (all met):
  1. Migrations 000007 (tool_permissions) and 000008 (audit_log) applied; CI updated to include them
  2. Admin can toggle tools per tenant via `PUT /admin/tenants/:id/tools`; disabled tools do not appear in MCP `tools/list`
  3. Admin can restrict a specific API key to a subset of tools via `PUT /admin/tenants/:id/keys/:keyId/tools`; MCP session sees intersection
  4. Every MCP tool call creates an audit_log row; admin can query via `GET /admin/tenants/:id/audit-log`
  5. `POST /admin/auth/login` returns a signed JWT; dashboard uses JWT for all requests; configurable expiry
  6. API keys accept optional `expires_at`; expired keys return 401 with "API key expired"
  7. All new backend services have integration tests (32 tests across 4 files)

### Phase 6: Admin Dashboard + Doc Upload

**Goal**: Admin can manage all aspects of the MCP server through a polished React dashboard — tenants, keys, tool permissions, ERP config, audit logs, and API documentation upload
**Depends on**: Phase 5
**Requirements**: DASH-01 to DASH-09, UPLOAD-01 to UPLOAD-04
**Plans:** 2 plans

Plans:
- [x] 06-01-PLAN.md — JWT auth integration, key expiry/scoping UI, audit filters, build pipeline scripts
- [x] 06-02-PLAN.md — KB doc CRUD backend endpoints, API Docs tab in dashboard

**Success Criteria** (all met):
  1. Dashboard login uses JWT; session persists across refresh; expired JWT redirects to login
  2. Tenant list loads all tenants with name, slug, plan, status, key count
  3. Tenant detail has working tabs: API Keys, Tool Permissions, ERP Config, Audit Log, API Docs
  4. API Docs tab: upload/view/edit/delete markdown docs; appear in MCP search_kb/get_kb_article
  5. `POST /admin/kb/upload` stores markdown with DOC-* prefix in kb_articles
  6. Dashboard production build served by Fastify via @fastify/static at `/dashboard/`
  7. Root package.json has `dashboard:dev` and `dashboard:build` scripts

### Phase 7: Write Tools

**Goal**: AI clients can create and modify stock entries, invoices, and contacts through MCP tools — all write operations go through the live POSibolt API with proper validation
**Depends on**: Phase 5 (audit logging wired), Phase 6 (dashboard for testing)
**Requirements**: WRITE-01 to WRITE-06
**Plans:** 2 plans

Plans:
- [x] 07-01-PLAN.md — Create 6 write tools (stock, invoice, contact CRUD), register in MCP server + ALL_TOOLS
- [x] 07-02-PLAN.md — Integration tests for write tools (registration, filtering, ALL_TOOLS)

**Success Criteria** (all met):
  1. `create_stock_entry` — POST /stocktransferrequest
  2. `update_stock_entry` — POST /stocktransfer/completestocktransfer
  3. `create_invoice` — POST /salesinvoice/createorderinvoice
  4. `update_invoice` — POST /salesorder/cancelorder
  5. `create_contact` — POST /customermaster (action:create)
  6. `update_contact` — POST /customermaster/{id} (action:update)
  7. All 6 audit-logged, access-controlled, structured errors on failure
  8. 27 total tools in ALL_TOOLS, manageable via dashboard

---

## Key Decisions

| Decision | Rationale |
|----------|-----------|
| JWT for dashboard auth | Raw admin secret in localStorage is insecure; JWT with expiry is standard practice |
| API key expiry (not OAuth 2.1) | OAuth 2.1 is over-engineered for M2M; expiry dates on API keys cover the security gap |
| Doc upload replaces YouTrack sync reliance | Simpler to manage; admin uploads markdown directly |
| Phase 5 before Phase 6 | Dashboard needs JWT auth and working backend services first |
| Write tools in Phase 7 | Requires audit logging (Phase 5) and dashboard for testing (Phase 6) |
| JWT via built-in crypto | Node.js crypto module handles HMAC-SHA256; avoids adding jose/jsonwebtoken dependency |
| JWT expiry default 8 hours | Configurable via JWT_EXPIRY_HOURS; covers a workday session |
| Login credentials reuse ADMIN_SECRET | ADMIN_USERNAME (default 'admin') + existing ADMIN_SECRET; no new secrets |
| DOC-* prefix for uploaded docs | Distinguishes from YouTrack-synced articles; allows CRUD guards |
| Doc content max 1MB | 1,048,576 character limit; prevents oversized payloads |

---

## Performance

| Plan | Duration | Tasks | Files |
|------|----------|-------|-------|
| 05-01 | 5m | 2 | 8 |
| 05-02 | ~3m | 2 | 10 |
| 05-03 | 3m | 2 | 4 |
| 06-01 | ~4m | 3 | 8 |
| 06-02 | ~3m | 2 | 3 |
| 07-01 | ~4m | 2 | 4 |
| 07-02 | ~1m | 1 | 1 |

---
*Archived: 2026-03-18*
